# Import behavior

Ако един работен номер се среща повторно в същия Excel файл, системата не създава втори служител. По-късният запис за същия служител и дата се приема като корекция, а броят на различаващите се дни се връща като `conflicting_days` в резултата от административния import endpoint.
